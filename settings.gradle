// Added since from version 7 onwards gradle requires explicitly plugin fetch location
pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        google()
    }
}

rootProject.name = "cloud-data-access-client"
